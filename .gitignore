# media & renders (multi-GB)
sample_vids/
*.MP4
*.mp4
*.gcsv

# analysis harness caches & golden dumps
analysis/cache/
goldens/
*.npz
*.png

# python
__pycache__/
*.pyc

# rust
rust/target/

# tauri-build regenerates these capability-schema files every build
# (used only for IDE autocompletion of capabilities/*.json, not source)
rust/o4fix-app/gen/schemas/

# gyroflow projects are kept: they're small JSON eval configs
!*.gyroflow
